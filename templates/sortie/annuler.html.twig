{% extends 'base.html.twig' %}

{% block title %}Annuler une sortie{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="container col-4">
        <h1>Détails de la sortie :</h1>

        <div> Nom de la sortie : {{ sortie.nom }}</div>
        <br>
        <div> Campus : {{ sortie.campus.nom }}</div>
        <br>
        <div> Date et heure de sortie : {{ sortie.dateHeureDebut|date("d-m-Y") }}</div>
        <br>
        <div> Lieu : {{ sortie.lieu.rue, sortie.lieu.ville.codePostal }}</div>
        <br>
        <div> Code postal : {{ sortie.lieu.ville.codePostal }}</div>
        {# <div> latitude : {{ sortie.infosSortie }}</div> #}
        {# <div> longitude : {{ sortie.infosSortie }}</div> #}
        <br>
        <label for="motif">Motifs : </label>
        <textarea id="motif" , name="motif" rows="7" cols="10">

    </textarea>


    </div>


    <button type="submit" class="btn btn-sucess">Enregistrer</button>

    <button type="button" class="btn btn-sucess">Annuler <a href="{{ path('accueil') }}">

        </a></button>



    </div>
{% endblock %}
